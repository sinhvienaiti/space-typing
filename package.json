{
  "name": "space-typing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3004",
    "build": "node scripts/check-audio-assets.mjs && node scripts/check-background-assets.mjs && tsc -p tsconfig.json --noEmit && vite build && node scripts/report-bundle-metrics.mjs && node scripts/check-ship-art-integrity.mjs",
    "test": "vitest run",
    "check": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.21.0"
}
