{
  "name": "space-typing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-ship-v3.mjs && vite --host 127.0.0.1 --port 3004",
    "build": "node scripts/prepare-ship-v3.mjs && tsc -p tsconfig.json --noEmit && vite build && node scripts/check-bundle-size.mjs && node scripts/check-ship-art-budget.mjs",
    "test": "vitest run",
    "check": "pnpm test && pnpm build"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.21.0"
}
